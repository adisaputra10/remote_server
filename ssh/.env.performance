# Performance optimized environment configuration
# Set log level to reduce I/O overhead (INFO, WARN, ERROR, or DEBUG for troubleshooting)
LOG_LEVEL=INFO

# Database configuration for better performance
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=logs

# Optional: Disable SSH command logging for maximum performance
# DISABLE_SSH_LOGGING=true

# Optional: Disable database query logging for maximum performance  
# DISABLE_QUERY_LOGGING=true

# Server configuration
PORT=8080

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# User credentials
USER_USERNAME=user
USER_PASSWORD=user123